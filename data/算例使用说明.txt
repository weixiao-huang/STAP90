算例分为两种，均以 .in结尾

一种是没有ANS-前缀的，跟正常的输入文件相同，需要在程序中，指定好文件所在的路径，也指定好文件的名字



一种是有ANS-前缀的，表示是利用ans得到的文件，可以通过前处理程序来得到输入文件
需要进入主程序stap.f90中，将读取文件的函数
由  CALL OPENFILES()
改为CALL PREOPENFILES()
此时只需要指定文件所在路径，并不需要指定文件名